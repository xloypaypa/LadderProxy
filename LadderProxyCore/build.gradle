group 'xlo'
version '1.0'

apply plugin: 'java'

repositories {
    mavenLocal()
    jcenter()
}

dependencies {
    compile "ladder:ladder:1.5.0"
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
